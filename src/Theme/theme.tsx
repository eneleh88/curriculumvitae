import { extendTheme } from "@chakra-ui/react";

export const extendedTheme = extendTheme({
    styles: {       
        global: {
          body: {
            bg: '#fbcb74'
          },
        },
      },
    });